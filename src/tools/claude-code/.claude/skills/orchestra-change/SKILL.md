---
name: orchestra-change
description: Use for Orchestra feature, bug, or refactor work that needs planning, explicit approval, implementation, verification, and docs/memory closeout.
---

# Orchestra Change

1. Read `/AGENTS.md`, `.ai/workflows/change.md`, `.ai/RULES.md`, and the role file for the current phase.
2. Keep context lean: name files instead of pasting canonical docs, include `Active overlays`, and include `Do not load` for broad folders or unrelated adapters.
3. Do not implement until an inline or `.ai/plans/` plan artifact is explicitly approved.
4. After implementation, run the most relevant verification command and report `doc impact` and `memory impact`.
