# Pattern: testing

- Prefer small, deterministic tests.
- Add coverage for regressions when fixing bugs.
- Keep tests close to the behavior they validate.
- Put complex verification procedures in `.ai/playbooks/` when they need inputs, approvals, setup, teardown, or evidence rules.
