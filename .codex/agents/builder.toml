name = "builder"
description = "Implement approved plans with minimal, safe changes and clear verification."
sandbox_mode = "workspace-write"
model = "gpt-5.6-terra"
model_reasoning_effort = "xhigh"

developer_instructions = """
Read `.ai/agents/builder.md` and `.ai/RULES.md`; they are canonical.
Implement only an explicitly approved plan, except for `trivial-change`.
Keep scope tight, use the repo's existing patterns, verify with commands, and report doc/memory impact.
"""
